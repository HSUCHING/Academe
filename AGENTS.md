# Academe Agent 工作规则

本文件适用于整个 Academe 仓库。所有 Agent 在分析、修改、测试和提交本仓库内容时必须遵守。

## 核心原则：保持上游可合并性

Academe 是 LearnHouse 的 fork。首要目标是在实现 Academe 需求的同时，尽量保持上游源码不变，使未来拉取和合并母仓库更新时冲突最少。

1. `apps/`、`packages/`、根 `Dockerfile` 及其他上游维护文件默认视为不可修改区域。
2. 优先通过 `deploy/`、环境变量、Docker Compose、反向代理、独立配置、适配器、外挂服务或 Academe 自建文件实现需求。
3. 修改上游源码前，必须先确认外围方案无法合理解决，并在工作说明中写明为什么必须修改源码。
4. 无法避免源码修改时，只修改完成目标所需的最少文件和最少行数；不得顺手重构、批量格式化、重命名或清理无关代码。
5. 新增逻辑应尽可能放在隔离的 Academe 自建文件中，让上游文件只保留最小接入点。

## 修改与记录要求

1. 开始修改前检查 `git status` 和相关 diff，保留用户已有改动，不覆盖、不回退无关内容。
2. 每项上游源码修改必须同步更新 `ACADEME_CUSTOMIZATIONS.md`，记录：
   - 修改原因和解决的问题；
   - 修改及新增的文件；
   - 为什么不能通过外围层解决；
   - 与上游发生冲突的风险；
   - 回退方法；
   - 测试和人工验收结果；
   - 上游出现等价修复后删除 Academe 补丁的条件。
3. Academe 自建目录、配置和部署文件也必须登记到该文档的目录字典。
4. 上游同步提交与 Academe 定制提交必须分开，便于审阅、rebase、cherry-pick 和回退。
5. 所有提交信息使用 Gitmoji；未经用户明确确认，不得推送远端。

## 验证与安全要求

1. 对修改运行与风险相称的测试，并将“已通过”“失败”“未执行”分别报告，不得把局部测试通过描述成全项目通过。
2. 不得提交密码、Token、API Key、数据库、上传内容、备份、Tinybird 登录状态或其他运行数据。
3. 合并上游更新时，逐项核对 `ACADEME_CUSTOMIZATIONS.md` 中的源码补丁；若上游已有等价实现，应优先移除本地补丁。
